<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<select id="getId" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_ID
		FROM MEMBER_INFO
		WHERE CONTACT = #{m_Phone}
		AND MEMBER_BIRTH = #{m_Birth}
		AND E_NAME = #{m_Email}
		AND E_ADDRESS = #{m_Address}
	</select>
	
	<select id="getPw" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_PW
		FROM MEMBER_INFO
		WHERE EMAIL = #{M_ID}
		AND E_NAME = #{m_Email}
		AND E_ADDRESS = #{m_Address}
	</select>
	
	<select id="getM" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_NO, NICKNAME
		FROM MEMBER_INFO
		WHERE MEMBER_ID = #{mId}
		AND MEMBER_PW = #{mPw}
	</select>
	
	<insert id="joinM" parameterType="hashmap" >
		INSERT INTO MEMBER_INFO(MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_BIRTH, GENDER, CONTACT, NICKNAME, LEAVE_CHECK, E_NAME, E_ADDRESS)
		VALUES(MEMBER_INFO_SEQ.NEXTVAL, #{mId},#{mPw},#{mBirth},#{mGender},#{mPhone},#{mNick},'1',#{mEmail},#{address})
	</insert>
	
	<select id="idCheck" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_ID
		FROM MEMBER_INFO
		WHERE MEMBER_ID = #{mId}
	</select>
	
	<select id="mCheck" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_ID
		FROM MEMBER_INFO
		WHERE CONTACT = #{m_Phone}
		AND MEMBER_BIRTH = #{m_Birth}
		AND E_NAME = #{m_Email}
		AND E_ADDRESS = #{m_Address}
	</select>
	
	<select id="mCheck" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_NO
		FROM MEMBER_INFO
		WHERE MEMBER_ID = #{mId}
		AND E_NAME = #{m_Email}
		AND E_ADDRESS = #{m_Address}
	</select>
	
</mapper>