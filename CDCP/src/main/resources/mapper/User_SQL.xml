<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<select id="getId" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_ID
		FROM MEMBER_INFO 
		WHERE EMAIL = #{checkEmail}
	</select>
	
	<select id="getM" parameterType="hashmap" resultType="hashmap">
		SELECT MEMBER_NO, NICKNAME
		FROM MEMBER_INFO
		WHERE MEMBER_ID = #{mId}
		AND MEMBER_PW = #{mPw}
	</select>
	
	<select id="joinM" parameterType="hashmap" resultType="hashmap">
		INSERT INTO MEMBER_INFO(MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_BIRTH, GENDER, CONTACT, NICKNAME, EMAIL, LEAVE_CHECK)
		VALUES(MEMBER_INFO_SEQ.NEXTVAL, #{mId},#{mPw},#{mBirth}, #{mGender},#{mPhone},#{mNick},#{mEmail},'1')
	</select>
</mapper>